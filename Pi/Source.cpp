#define _USE_MATH_DEFINES
#include <cmath>

static char v[stack_size];

static char* p = v;
void push(char c) 
{
	char pop()
	{

  }
}

double sqrt(double arg)
{

}

void some_function()
{
	double root = sqrt(2);
	push('c');
	char c = pop();
	if (c != 'c')error("ni");
}


void push(char);
char pop();

const int stack_size = 100;